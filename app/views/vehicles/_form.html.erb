<%= simple_form_for @vehicle do |form| %>
  <% if vehicle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vehicle.errors.count, "error") %> prohibited this vehicle from being saved:</h2>

      <ul>
      <% vehicle.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.input :vin %>
  </div>

  <div class="field">
    <%= form.input :model %>
  </div>

  <div class="field">
    <%= form.input :mark %>
  </div>

  <div class="field">
    <%= form.input :year %>
  </div>

  <div class="field">
    <%= form.input :colour %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
