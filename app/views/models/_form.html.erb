<%= simple_form_for(@model) do |f| %>
<%= f.error_notification %>
<%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

<%= f.input :name %>

<%= f.association :mark,label_method: :name, value_method: :id %>

<%= f.button :submit %>
<% end %>
