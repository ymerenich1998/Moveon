<%= simple_form_for @order do |form| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.input :worker_id %>
  </div>

  <div class="field">
    <%= form.input :customer_id %>
  </div>

  <div class="field">
    <%= form.input :service_id %>
  </div>

  <div class="field">
    <%= form.input :vehicle_id %>
  </div>

  <div class="field">
    <%= form.input :date %>
  </div>

  <div class="field">
    <%= form.input :status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
